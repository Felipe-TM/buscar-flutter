import 'package:flutter/material.dart';

class RidePassangersList extends StatelessWidget {
  const RidePassangersList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
